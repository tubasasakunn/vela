# Vela 開発ルール

## 設計

- `main.swift` は起動とコマンド振り分けだけに保ち、機能の実装を置かない。
- App、CLI、Core の責務を混ぜない。UI と macOS 連携は App/CLI、再利用できる状態・判定・モデルは Core に置く。
- まず標準フレームワークと小さな値型・関数で解決する。新しい抽象化は、重複または変更理由を実際に減らす場合だけ追加する。
- 到達不能な分岐、使われないモデル、同じ副作用を二重に起こす通知経路を残さない。
- 既存の公開 API、JavaScript 設定形式、CLI、通知名との互換性は、明示的な破壊的変更でない限り維持する。
- Vela の設定 API・設定可能な機能・検証手順を追加または変更したら、`ConfigurationSkill` が配布する `<設定フォルダ>/AGENT.md` と `<設定フォルダ>/.agent/skills/vela-configuration/`（API リファレンスを含む）も同じ変更で更新し、内容と実装を一致させる。
- 権限判定や設定検証などの方針は、UI 表示から分離して単体テスト可能にする。
- 関係のない整形や改名を混ぜず、変更差分から意図を説明できる状態を保つ。
- 新しいコンパイラ警告を追加しない。既存の警告を変更範囲で解消できない場合は、完了報告に明記する。

## 検証

- コード、設定モデル、ビルドスクリプト、CLI、App の挙動を変更したら、commit 前に `$vela-app-verification` スキルを使う。
- スキルに記載された自動検証をすべて通し、App または CLI に触れた変更では実際に署名済み App を起動して該当 UI/IPC を確認する。
- macOS 権限や Apple Intelligence など環境依存の項目を確認できない場合、成功扱いにせず未確認項目として明記する。
- テスト失敗や `git diff --check` の問題を残したまま commit しない。
